---
# Cloud Manager
apiVersion: projectcontour.io/v1
kind: HTTPProxy
metadata:
  name: management-admin
  namespace: NAMESPACE_PLACEHOLDER
spec:
  virtualhost:
    fqdn: admin.DNS_PLACEHOLDER
    tls:
      secretName: cm-endpoint
  routes:
  - services:
    - name: management-juhu
      port: 2005
      protocol: tls
      validation:
        caSecret: management-ca
        subjectName: management-juhu.NAMESPACE_PLACEHOLDER.svc.cluster.local
---
# API Manager
apiVersion: projectcontour.io/v1
kind: HTTPProxy
metadata:
  name: management-api-manager
  namespace: NAMESPACE_PLACEHOLDER
spec:
  virtualhost:
    fqdn: manager.DNS_PLACEHOLDER
    tls:
      secretName: apim-endpoint
  routes:
  - services:
    - name: management-juhu
      port: 2006
      protocol: tls
      validation:
        caSecret: management-ca
        subjectName: management-juhu.NAMESPACE_PLACEHOLDER.svc.cluster.local
---
# Platform API
apiVersion: projectcontour.io/v1
kind: HTTPProxy
metadata:
  name: management-platform-api
  namespace: NAMESPACE_PLACEHOLDER
spec:
  virtualhost:
    fqdn: api.DNS_PLACEHOLDER
    tls:
      secretName: api-endpoint
  routes:
  - services:
    - name: management-juhu
      port: 2000
      protocol: tls
      validation:
        caSecret: management-ca
        subjectName: management-juhu.NAMESPACE_PLACEHOLDER.svc.cluster.local
---
# Consumer API
apiVersion: projectcontour.io/v1
kind: HTTPProxy
metadata:
  name: management-consumer-api
  namespace: NAMESPACE_PLACEHOLDER
spec:
  virtualhost:
    fqdn: consumer.DNS_PLACEHOLDER
    tls:
      secretName: consumer-endpoint
  routes:
  - services:
    - name: management-juhu
      port: 2001
      protocol: tls
      validation:
        caSecret: management-ca
        subjectName: management-juhu.NAMESPACE_PLACEHOLDER.svc.cluster.local
---
# Consumer Catalog
apiVersion: projectcontour.io/v1
kind: HTTPProxy
metadata:
  name: management-consumer-catalog
  namespace: NAMESPACE_PLACEHOLDER
spec:
  virtualhost:
    fqdn: consumer-catalog.DNS_PLACEHOLDER
    tls:
      secretName: consumer-catalog-endpoint
  routes:
  - services:
    - name: management-juhu
      port: 2013
      protocol: tls
      validation:
        caSecret: management-ca
        subjectName: management-juhu.NAMESPACE_PLACEHOLDER.svc.cluster.local
